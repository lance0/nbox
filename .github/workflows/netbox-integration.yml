name: NetBox Integration

# Heavy end-to-end checks against REAL, pinned NetBox fixtures. Kept separate
# from the fast `ci.yml` (fmt/clippy/build/unit tests) because it boots a full
# NetBox stack and seeds a fixture. The gated tests in `tests/it_netbox.rs` are
# `#[ignore]`d, so they never run in the normal suite — only here, via
# `--ignored`.
#
# Note: making this a *required* status check is a GitHub branch-protection
# setting configured in the repo UI, not in this file.

on:
  # Skip doc-only (Markdown) changes — this boots a full NetBox stack, so there's
  # nothing to validate when only docs changed.
  push:
    branches: [master]
    paths-ignore:
      - '**.md'
  pull_request:
    paths-ignore:
      - '**.md'
  schedule:
    # Nightly compatibility drift check for the pinned NetBox lines below.
    - cron: "17 9 * * *"
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always
  # The pinned, deterministic superuser API token. MUST match
  # tests/integration/docker-compose.yml's SUPERUSER_API_TOKEN and seed.py.
  NETBOX_TOKEN: "0123456789abcdef0123456789abcdef0fedcba9"
  NBOX_URL: "http://localhost:8000"

jobs:
  netbox-integration:
    name: live NetBox 4.2 end-to-end
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Cache cargo artifacts
        uses: Swatinem/rust-cache@v2

      # Build the binary up front: `cargo test --test it_netbox` will reuse it,
      # and building before NetBox is needed keeps the boot wait productive.
      - name: Build nbox
        run: cargo build --all-features --verbose

      - name: Boot NetBox 4.2 fixture
        run: docker compose -f tests/integration/docker-compose.yml up -d

      - name: Wait for NetBox to be ready
        run: ./tests/integration/wait-for-ready.sh 420

      - name: Seed the fixture
        run: ./tests/integration/seed.py

      - name: Run gated integration tests
        run: cargo test --test it_netbox -- --ignored --skip graphql_backend

      # Always surface NetBox logs on failure to debug a flaky boot/seed.
      - name: Dump NetBox logs on failure
        if: failure()
        shell: bash
        run: |
          mkdir -p .ci-logs
          docker compose -f tests/integration/docker-compose.yml logs --no-color netbox \
            | tee .ci-logs/netbox-4.2.log

      - name: Upload NetBox logs on failure
        if: failure()
        uses: actions/upload-artifact@v7
        with:
          name: netbox-4.2-logs
          path: .ci-logs/*.log
          if-no-files-found: ignore

      - name: Tear down
        if: always()
        run: docker compose -f tests/integration/docker-compose.yml down -v

  netbox-45-graphql:
    name: live NetBox 4.5 GraphQL backend
    runs-on: ubuntu-latest
    env:
      NETBOX_IMAGE: netboxcommunity/netbox:v4.5.10-4.0.2
    steps:
      - uses: actions/checkout@v7

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Cache cargo artifacts
        uses: Swatinem/rust-cache@v2

      - name: Build nbox
        run: cargo build --all-features --verbose

      - name: Boot NetBox 4.5 fixture
        run: docker compose -f tests/integration/docker-compose.yml up -d

      - name: Wait for NetBox 4.5 to finish first boot
        run: NETBOX_READY_HTTP_CODES=200,403 ./tests/integration/wait-for-ready.sh 420

      - name: Resolve NetBox 4.5 API token
        run: echo "NETBOX_TOKEN=$(./tests/integration/resolve-token.sh)" >> "$GITHUB_ENV"

      - name: Wait for NetBox to be ready
        run: ./tests/integration/wait-for-ready.sh 60

      - name: Seed the fixture
        run: ./tests/integration/seed.py

      - name: Run gated GraphQL integration tests
        run: cargo test --test it_netbox graphql_backend -- --ignored --test-threads=1

      - name: Dump NetBox logs on failure
        if: failure()
        shell: bash
        run: |
          mkdir -p .ci-logs
          docker compose -f tests/integration/docker-compose.yml logs --no-color netbox \
            | tee .ci-logs/netbox-4.5.log

      - name: Upload NetBox logs on failure
        if: failure()
        uses: actions/upload-artifact@v7
        with:
          name: netbox-4.5-logs
          path: .ci-logs/*.log
          if-no-files-found: ignore

      - name: Tear down
        if: always()
        run: docker compose -f tests/integration/docker-compose.yml down -v

  netbox-46-compat:
    name: live NetBox 4.6 compatibility
    if: github.event_name == 'schedule' || github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    env:
      NETBOX_IMAGE: netboxcommunity/netbox:v4.6.2
    steps:
      - uses: actions/checkout@v7

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Cache cargo artifacts
        uses: Swatinem/rust-cache@v2

      - name: Build nbox
        run: cargo build --all-features --verbose

      - name: Boot NetBox 4.6 fixture
        run: docker compose -f tests/integration/docker-compose.yml up -d

      - name: Wait for NetBox 4.6 to finish first boot
        run: NETBOX_READY_HTTP_CODES=200,403 ./tests/integration/wait-for-ready.sh 420

      - name: Resolve NetBox 4.6 API token
        run: echo "NETBOX_TOKEN=$(./tests/integration/resolve-token.sh)" >> "$GITHUB_ENV"

      - name: Wait for NetBox to be ready
        run: ./tests/integration/wait-for-ready.sh 60

      - name: Seed the fixture
        run: ./tests/integration/seed.py

      - name: Run gated compatibility tests
        run: cargo test --test it_netbox -- --ignored --test-threads=1 --skip status_reports_netbox_4_2

      - name: Dump NetBox logs on failure
        if: failure()
        shell: bash
        run: |
          mkdir -p .ci-logs
          docker compose -f tests/integration/docker-compose.yml logs --no-color netbox \
            | tee .ci-logs/netbox-4.6.log

      - name: Upload NetBox logs on failure
        if: failure()
        uses: actions/upload-artifact@v7
        with:
          name: netbox-4.6-logs
          path: .ci-logs/*.log
          if-no-files-found: ignore

      - name: Tear down
        if: always()
        run: docker compose -f tests/integration/docker-compose.yml down -v
